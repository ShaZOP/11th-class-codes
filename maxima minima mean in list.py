#maxima minima in a given list
sum_num = 0
list1 = eval(input("Enter a list"))
maxima = print("Maxima of the list is ",max(list1))
minima = print("Minima of the list is ",min(list1))
for t in list1:
        sum_num = sum_num + t
avg = sum_num / len(num)        
