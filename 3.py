num = int(input(""))
sq = int(num**0.5)
if sq*sq == num:
    print("yes")
    numstr = str(num)
    firstsq = int(num[:2])
    lastsq = int(num[-2:])
     if first_sqrt * first_sqrt == int(num_str[:2]) and last_sqrt * last_sqrt == int(num_str[-2:]):
        print("The first 2 digits and last 2 digits of", num, "are perfect squares.")
    else:
        print("The first 2 digits and last 2 digits of", num, "are not perfect squares.")
    
